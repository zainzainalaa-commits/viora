{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "hdr-overlay",
  "description": "HDR controls overlay window: events + window/webview control for the chrome. Deliberately omits filesystem, shell and open-network permissions so this window cannot reach the fs-write / shell surface.",
  "platforms": ["windows", "macOS", "linux"],
  "windows": ["harbor-hdr-overlay"],
  "permissions": [
    "core:default",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-start-dragging",
    "core:window:allow-is-maximized",
    "core:window:allow-set-decorations",
    "core:window:allow-set-fullscreen",
    "core:window:allow-is-fullscreen",
    "core:window:allow-start-resize-dragging",
    "core:window:allow-set-size",
    "core:window:allow-set-position",
    "core:window:allow-current-monitor",
    "core:window:allow-outer-size",
    "core:window:allow-outer-position",
    "core:webview:allow-set-webview-zoom"
  ]
}
